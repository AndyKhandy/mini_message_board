<html>
    <head>
        <link rel="stylesheet" href="/styles.css">
    </head>
    <body>
        <h1><%= title %></h1>
        <% messages.forEach((message, index) => { %>
            <h2>username <%= (index+1) %></h2>
            <%- include('messageInfo', {message}) %>
            <a href=<%= index%>>Open</a>
        <% }) %>
        <div id="createNew">
            <a href="/new">Create new message!</a>
        </div>
    </body>
</html>